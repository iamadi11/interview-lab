{
  "name": "@interview-lab/python-playground",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "echo 'Run: python -m uvicorn main:app --reload --port 8000'",
    "start": "python main.py",
    "setup": "python -m venv .venv && .venv/bin/pip install -r requirements.txt",
    "clean": "rm -rf __pycache__ .venv"
  }
}
